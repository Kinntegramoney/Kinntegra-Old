import { FileDisplayNamePipe } from './file-display-name.pipe';

describe('FileDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FileDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
